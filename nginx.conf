events {}

http {
    limit_req_zone $binary_remote_addr zone=one:10m rate=10r/s;
    server {
        listen 443 ssl;

        location / {
            limit_req zone=one;
            proxy_pass http://users_back:8000/;
        }

        ssl_certificate /etc/letsencrypt/live/booking-fastapi-project.ru/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/booking-fastapi-project.ru/privkey.pem;
        include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
        ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot
    }
}